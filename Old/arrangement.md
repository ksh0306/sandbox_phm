phm_data.py를 참고로 하여 다음의 작업을 실시한다.(다른 파일은 볼 필요 없음.)
각 기능의 재사용성을 높이는 것에 가장 큰 주안점을 둔다.
하기는 파일을 정리하여 git에 pr을 올린 후 받은 피드백이다. 
"""
현재 프로젝트 구조상 FFT 변환 코드가 다른 로직들과 함께 섞여 있어서, 추후 다른 프로젝트에서 재사용하기에는 조금 어려울 수도 있을 것 같다는 생각이 들었습니다.
혹시 아래와 같이 역할별로 class를 분리해보는 방향은 어떨지 의견 여쭤보고 싶습니다 😄

FFT 변환 class (inverse FFT 포함)
FFT 결과를 initial value로 받아 visualization을 담당하는 class
FFT 결과를 initial value로 받아 h5 파일로 저장하는 class
그리고 마지막 h5 저장 부분은, FFT 변환 자체에 시간이 꽤 소요되어서 결과를 save/load 형태로 재사용하기 위한 목적이 맞는지도 궁금합니다!
"""
0. 기존의 데이터파일(h5포맷)들을 event, fast_flags, fast_motor_state, lifetime, motor_spec, slow_ctx, ts_us까지 읽어들이고 저장하는 클래스를 따로 생성한다.(저장 파일명 지정가능)
(v1,v2,v3,i1,i2,i3 데이터는 한개 데이터에 0-5 필드에 저장하라.)
1. 위의 클래스를 상속받아, Raw데이터를 저장하는 클래스를 따로 생성한다.
2. 해당 클래스는 그대로 h5 포맷 파일로 저장하는 기능을 갖고 있어야 하며, 다른 전처리를 위한 데이터 접근이 가능해야 한다.
3. 위의 클래스 인스턴스를 받아 LOG16P 연산처리를 할 수 있는 함수를 따로 구현한다.(다른 전처리 함수도 계속 추가할 수 있도록 한다.)
-- 여기까지 하고 실행하여 새로운 파일을 생성하여 확인할 수 있도록 한다.
4. FFT 변환된 데이터를 저장하는 클래스를 0번 작업된 클래스를 상속받아 생성한다.
5. Raw Class Instance를 받아 윈도크기를 정해서 FFT를 수항하여 4번 클래스 인스턴스를 생성하는 함수를 4번 클래스의 정적 함수로 구현한다.
-- 여기까지 하고 실행하여 Raw와 Scaled 데이터를 FFT로 변환한 새로운 파일을 생성하여 확인할 수 있도록 한다.
6. Raw데이터(Scaled 데이터도 마찬가지) 인스턴스를 받아서 v1-v2, v2-v3, v3-v1, i1-i2, i2-i3, i3-i1, v1-i1, v2-i2, v3-i3 의 cross-correlation 변환된 데이터를 저장하는 클래스를 생성한다.(개별 데이터로 저장하지 말고, 각각 0-8 필드에 저장하라.)
7. FFT데이터 인스턴스를 받아서 v1-v2, v2-v3, v3-v1, i1-i2, i2-i3, i3-i1, v1-i1, v2-i2, v3-i3 의 cross-correlation 변환된 데이터를 저장하는 클래스를 생성한다.
(개별 데이터로 저장하지 말고, 각각, 0-8번 필드에 저장하라.)
8. 6,7번 각각의 변환을 수행하는 정적함수를 구현한다.
-- 여기까지 하고 실행하여 새로운 파일을 생성하여 확인할 수 있도록 한다.
